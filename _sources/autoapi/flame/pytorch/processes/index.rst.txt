:py:mod:`flame.pytorch.processes`
=================================

.. py:module:: flame.pytorch.processes


Submodules
----------
.. toctree::
   :titlesonly:
   :maxdepth: 1

   amp_process/index.rst
   process/index.rst
   supervised_process/index.rst


Package Contents
----------------

Classes
~~~~~~~

.. autoapisummary::

   flame.pytorch.processes.Process
   flame.pytorch.processes.AmpProcess
   flame.pytorch.processes.SupervisedProcess
   flame.pytorch.processes.SupervisedAmpProcess




.. py:class:: Process

   .. py:method:: forward(self, batch)

      forward wrapper

      处理数据并计算loss，决定返回哪些数据。

      :returns: 必须包含loss和batch_size
      :rtype: output

      example::

          def forward(self, batch):
              image, label = batch
              pred = self.model(image)
              loss = self.criterion(pred, label)
              return self.output(loss=loss, batch_size=labe.size(0), pred=pred, label=label)



   .. py:method:: training_step(self, batch)


   .. py:method:: validation_step(self, batch)


   .. py:method:: update(self)


   .. py:method:: train(self, mode = True)


   .. py:method:: eval(self)


   .. py:method:: output(loss = None, batch_size = None, **kwargs)
      :staticmethod:


   .. py:method:: infer_dtype(module)
      :staticmethod:

      通过model推断dtype


   .. py:method:: infer_device(module)
      :staticmethod:

      通过model推断device



.. py:class:: AmpProcess(scaler)

   Bases: :py:obj:`flame.pytorch.processes.process.Process`

   .. py:method:: training_step(self, batch)


   .. py:method:: is_amp_enabled(self)


   .. py:method:: autocast(self)



.. py:class:: SupervisedProcess(model, optimizer, criterion)

   Bases: :py:obj:`flame.pytorch.processes.process.Process`

   .. py:method:: forward(self, batch)

      forward wrapper

      处理数据并计算loss，决定返回哪些数据。

      :returns: 必须包含loss和batch_size
      :rtype: output

      example::

          def forward(self, batch):
              image, label = batch
              pred = self.model(image)
              loss = self.criterion(pred, label)
              return self.output(loss=loss, batch_size=labe.size(0), pred=pred, label=label)



   .. py:method:: train(self, mode = True)


   .. py:method:: update(self)

      为了兼容gradient accumulation，将zero_grad放最后



.. py:class:: SupervisedAmpProcess(model, optimizer, criterion, scaler)

   Bases: :py:obj:`flame.pytorch.processes.amp_process.AmpProcess`

   .. py:method:: forward(self, batch)

      forward wrapper

      处理数据并计算loss，决定返回哪些数据。

      :returns: 必须包含loss和batch_size
      :rtype: output

      example::

          def forward(self, batch):
              image, label = batch
              pred = self.model(image)
              loss = self.criterion(pred, label)
              return self.output(loss=loss, batch_size=labe.size(0), pred=pred, label=label)



   .. py:method:: train(self, mode = True)


   .. py:method:: update(self)

      为了兼容gradient accumulation，将zero_grad放最后



